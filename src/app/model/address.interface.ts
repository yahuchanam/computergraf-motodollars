export interface Address {
  id: number;
  address: string;
  addressNumber: string;
  extra: string;
  district: string;
  city: string;
  state: string;
  postalCode: string;
  kmDistance: number;
}
